const [ a, b, c, d ] = [ 12, 20, 3, 5 ];
const [ e, f, g, h ] = [ 12, 20, 3, 5 ];
let soma = (a + e)+(b + f)+(c + g)+(d + h);
console.log(soma);

let mult = (a * e)+(b * f)+(c * g)+(d * h);
console.log(mult);